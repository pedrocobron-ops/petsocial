/**
 * Largura máxima do feed (centralizado no desktop, edge-to-edge no mobile).
 * Inspirado no Instagram Web que limita a coluna a ~470-600px.
 */
export const MAX_FEED_WIDTH = 540;

/**
 * Margem horizontal padrão dos cards do feed.
 */
export const FEED_CARD_MARGIN = 14;
