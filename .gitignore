# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Pasta de trabalho do mascote (fontes/scripts/previews) — assets finais vão em public/mozart/
/Mozart/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files — protege .env (anon key é pública por design,
# mas boa prática é configurar via Vercel/Supabase Dashboard em vez de commitar)
.env
.env*.local

# typescript
*.tsbuildinfo

app-example

# generated native folders
/ios
/android

# Claude Code agent config (local)
.claude/

# Relatórios internos de auditoria — NÃO publicar em repo público
# (lista detalhada de bugs/vulnerabilidades + dados internos). Ler localmente.
AUDIT-REPORT.md
supabase/audit-fixes-v28.sql
scripts/.osm/
supabase/seed-osm-places.sql
